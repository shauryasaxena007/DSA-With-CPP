
    // insertNode(tail, 5,3);
    // print(tail);

    // insertNode(tail, 3,2);
    // print(tail);

    // insertNode(tail, 2,13);
    // print(tail);

    // insertNode(tail, 13,45);
    // print(tail);

    // insertNode(tail, 45,23);
    // print(tail);